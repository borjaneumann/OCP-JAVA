package _3_ApplyingAnnotations.CreatingAValueElement;

 //the following annotation declarations cannot be used with a shorthand annotation:
 // it does not include an element named value.
public @interface Wake {
    String hours();
}
