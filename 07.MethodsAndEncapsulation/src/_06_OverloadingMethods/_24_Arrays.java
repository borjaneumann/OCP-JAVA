package _06_OverloadingMethods;

public class _24_Arrays {
    /*
    Arrays have been around since the beginning of Java. They specify their
    actual types and don’t participate in type erasure.

    Unlike the previous example, this code is just fine:
        public static void walk(int[] ints) {}
        public static void walk(Integer[] integers) {}
     */
}
