package _1_CreatingObjects._02_ReadingAndWritingMemberFields;

public class Name {
    String first = "Theodore";
    String last = "Moose";
    String full = first + last;
    /*
    Lines 4 and 5 both write to fields. Line 5 both reads and writes data.
    It reads the fields first and last. It then writes the field full.
     */
}
