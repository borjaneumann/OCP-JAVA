public class _27_ReadingFileSizeWithSize {

    /*
    The Files class includes a method to determine the size of the file in
    bytes.
    public static long size(Path path) throws IOException

    The size returned by this method represents the conceptual size of the
    data, and this may differ from the actual size on the persistent storage
    device. The following is a sample call to the size() method:

    System.out.print(Files.size(Paths.get("/zoo/animals.txt")));
     */

}
